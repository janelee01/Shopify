{% comment %}
Auto-generated by Shogun.
This file can be re-written at any time.
{% endcomment %}

{% assign handles = content.metafields.shogun.products | split: "," %}
{% if handles.size > 0 %}
  <script>
    window.__shgProducts = window.__shgProducts || {};
    {% for handle in handles %}
      {% assign currentProduct = all_products[handle] %}
      window.__shgProducts["{{handle}}"] =  {% if currentProduct == blank %} null {% else %} {{currentProduct | json }} {% endif %};
    {% endfor %}
  </script>
{% endif %}
