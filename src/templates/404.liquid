<article>
	<header class="page-header">
		<h1>{{ 'general.404.title' | t }}</h1>
	</header>
	<div class="account-form">
		<p>{{ 'general.404.subtext_html' | t }}</p>
		<div id="in-page-search-container">
		  {%- assign fieldId = 'in-page-search' -%}
		  {% include 'search-form' with fieldId, placeholder %}
		</div>
	</div>
</article>