<!doctype html>
<!--[if IE 9]> <html class="ie9 no-js" lang="{{ shop.locale }}"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html class="no-js password-wall" lang="{{ shop.locale }}"> <!--<![endif]-->
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="{{ settings.color_primary }}">
  <link rel="canonical" href="{{ canonical_url }}">

  {% if settings.favicon != blank %}
    <link rel="shortcut icon" href="{{ settings.favicon | img_url: '32x32' }}" type="image/png">
  {% endif %}

  <title>{{ shop.name }}</title>

  {% if page_description %}
    <meta name="description" content="{{ page_description | escape }}">
  {% endif %}

  {% comment %}{% include 'social-meta-tags' %}{% endcomment %}

  {{ 'theme.scss.css' | asset_url | stylesheet_tag }}

  {{ content_for_header }}

</head>

<body class="template-password">
  <main role="main" id="MainContent">
    <div id="Login">
      <h1 id="site-logo" itemscope itemtype="http://schema.org/Organization">{{ shop.name }}</h1>
      <p>Sorry for the inconvenience, but we’re re-launching our store and will be back online to momentarily. If you happen to have a password for preview access, please enter it below.</p>
      {% comment %}<h4>{{ 'general.password_page.login_form_heading' | t }}</h4>{% endcomment %} 
      {% form 'storefront_password' %}
        {{ form.errors | default_errors }}
        <div class="form-group floating-label">
          <label for="password">{{ 'general.password_page.login_form_password_label' | t }}</label>
          <input type="password"
            name="password"
            id="Password"
            class="form-control"
            placeholder="{{ 'general.password_page.login_form_password_placeholder' | t }}">
        </div>
        <button class="btn btn-block" type="submit" name="commit">
          {{ 'general.password_page.login_form_submit' | t }}
        </button>
      {% endform %}
      {% comment %}<p>{{ 'general.password_page.admin_link_html' | t }}</p>{% endcomment %}
    </div>
    {% comment %}{{ content_for_layout }}{% endcomment %}
  </main>
</body>
</html>
